import ExpenseInput from '@/components/ExpenseInput';
import React from 'react';

const page = () => {
    return (
        <div>
            <ExpenseInput/>
        </div>
    );
};

export default page;