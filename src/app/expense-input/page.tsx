import ExpenseInput from '@/components/ExpenseForm';
import React from 'react';

const page = () => {
    return (
        <div>
            <ExpenseInput/>
        </div>
    );
};

export default page;